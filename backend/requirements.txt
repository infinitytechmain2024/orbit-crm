# Core / pure-Python deps first so they always install even if a heavy
# package below fails to build a wheel on the deployed Python version.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.4.0
requests>=2.31.0
httpx>=0.27.0
python-multipart>=0.0.9
supabase>=2.6.0
openai>=1.10.0

# Heavier / feature-specific deps (imported lazily or only by optional endpoints)
faster-whisper>=1.0.0
ollama>=0.3.0
playwright>=1.40.0
selenium>=4.15.0
beautifulsoup4>=4.12.0
# praisonaiagents is imported lazily inside backend/agents/liam_agent.py,
# so it is intentionally omitted here to avoid breaking the build.
