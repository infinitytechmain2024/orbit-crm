Ниже 4 промпта. Копируй по одному, по порядку — каждый следующий опирается на результат предыдущего.

---

**Промпт 1 — мозг (движок навыков + environment на задачу)**

```
## Objective
Создать внутри репозитория Orbit CRM подсистему `jarvis/` — маршрутизатор навыков и менеджер изолированных окружений выполнения, где каждая верхнеуровневая задача получает своё окружение, а подзадачи наследуют окружение родителя.

## Context
- Репозиторий: [ПУТЬ_ДО_РЕПО_ORBIT_CRM], стек: [СТЕК: например Next.js + Node + Postgres]
- Машина: macOS, Apple Silicon. Всё локально.
- Движок исполнения — Claude Code. Навык = папка с файлом SKILL.md, а не один большой промпт.
- Память (Obsidian) и голос будут подключены отдельными этапами. Сейчас нужны только скелет и маршрутизация.

## Target State
1. Создана структура:
   jarvis/
     skills/<skill-name>/SKILL.md
     router/            — выбор ОДНОГО навыка на запрос
     runs/              — окружения выполнения
     jarvis.config.json — пути, лимиты, реестр навыков
     README.md
2. Созданы 5 навыков, каждый с одной целью: `metrics`, `inbox`, `trends`, `plan`, `storage`.
   Каждый SKILL.md: YAML-frontmatter (name, description, triggers, inputs, outputs, max_runtime) + тело ≤ 150 строк.
   `description` формулируется так, чтобы маршрутизатор мог выбрать навык по нему однозначно.
3. Менеджер окружений:
   - Верхнеуровневая задача → `run_id = run_<YYYYMMDD-HHMM>_<slug>`, папка `jarvis/runs/<run_id>/` с `env.json`, `workspace/`, `log.jsonl`.
   - Подзадача передаёт `parent_run_id` и выполняется В ТОМ ЖЕ окружении и workspace — новое окружение не создаётся.
   - `env.json` хранит: run_id, parent_run_id, skill, статус, входы, созданные артефакты.
   - Политика очистки: runs старше N дней архивируются в `jarvis/runs/_archive/`, никогда не удаляются автоматически.
4. CLI-вход: `jarvis/bin/jarvis "<запрос>"` — печатает выбранный навык, run_id и путь к workspace.

## Scope
- Работать ТОЛЬКО в: jarvis/
- НЕ трогать: существующий код Orbit CRM, .env, миграции БД, lock-файлы, CI-конфиги.

## Constraints
- Никаких новых зависимостей без явного запроса у меня. Приоритет — stdlib и то, что уже есть в репо.
- Маршрутизатор выбирает ровно один навык. Если уверенность низкая — возвращает вопрос на уточнение, а не угадывает.
- Мелкий навык с одной целью лучше, чем универсальный. Не объединяй навыки.
- Делай только то, что запрошено. Не добавляй авторизацию, UI, телеметрию, абстракции «на будущее».

## Acceptance Criteria
- [ ] `jarvis/bin/jarvis "покажи метрики за неделю"` выбирает `metrics` и создаёт ровно одну папку в runs/
- [ ] Запуск подзадачи с `--parent <run_id>` не создаёт новую папку и пишет в тот же log.jsonl
- [ ] Каждый из 5 SKILL.md проходит валидацию frontmatter скриптом `jarvis/router/validate.py`
- [ ] Ни один файл вне jarvis/ не изменён (подтверди через `git status`)
- [ ] README.md описывает, как добавить шестой навык за 5 минут

## Action Boundaries
- Обратимые действия внутри jarvis/ выполняй сам.
- Остановись и спроси перед: установкой любой зависимости, изменением файлов вне jarvis/, любыми операциями с БД, `git commit`/`push`.

## Progress Evidence
Отчитывайся только на чекпоинтах. Каждое утверждение «готово» подкрепляй результатом команды или диффом файла — не описанием намерения.

## Session Strategy
Новая сессия. Начни с чтения структуры репозитория, чтобы понять существующие конвенции именования, и следуй им.
```

🎯 Target: Claude Code, 💡 Зафиксирован жёсткий scope-lock на `jarvis/` и явное правило наследования окружения подзадачами — именно то место, где агенты обычно плодят лишние среды.

---

**Промпт 2 — память на Obsidian**

```
## Objective
Подключить локальный Obsidian-vault как долговременную память Jarvis: трёхслойное хранилище raw → wiki → output с автоматической связностью заметок.

## Context
- Vault: [ПУТЬ_ДО_OBSIDIAN_VAULT] (локальная папка, доступ через файловую систему)
- Подсистема jarvis/ уже создана в репо Orbit CRM: есть router, runs, 5 навыков (metrics, inbox, trends, plan, storage)
- macOS, Apple Silicon. Синхронизация vault — сторонняя, наш код в неё не вмешивается.

## Target State
1. В vault создаются три слоя (если их нет):
   - `00_raw/` — всё захваченное как есть: транскрипты, вставки, входящие. Никогда не редактируется задним числом.
   - `10_wiki/` — очищенные атомарные заметки: один факт/сущность = одна заметка.
   - `20_output/` — всё, что произвёл Jarvis: планы, отчёты, черновики.
2. Каждая создаваемая заметка имеет frontmatter: `id`, `created`, `source`, `layer`, `run_id`, `tags`, `links`.
3. Модуль `jarvis/memory/`:
   - `capture()` — пишет в 00_raw с дедупликацией по хешу содержимого
   - `promote()` — превращает raw-заметку в атомарные wiki-заметки, проставляет `[[wikilinks]]` на уже существующие заметки vault, никогда не перезаписывает существующую wiki-заметку без явного подтверждения
   - `emit()` — сохраняет результат навыка в 20_output и линкует его на использованные wiki-заметки
   - `recall(query)` — локальный поиск по vault, возвращает ранжированные заметки с путями
4. Навык `storage` из промпта 1 переключён на этот модуль.
5. Индекс `10_wiki/_index.md` с картой основных тем, обновляется при promote.

## Scope
- Работать в: jarvis/memory/, jarvis/skills/storage/
- В vault — только СОЗДАВАТЬ файлы в трёх указанных папках.
- НЕ трогать: существующие заметки пользователя вне 00_raw/10_wiki/20_output, `.obsidian/` и его плагины, любой код Orbit CRM.

## Constraints
- Только Markdown + YAML frontmatter. Никакой внешней БД, никаких индексов в бинарном формате без запроса.
- Удаление заметок запрещено полностью. Устаревшее помечается `status: archived` и переносится в `_archive/` внутри своего слоя.
- Ни один вызов не уходит в интернет. Память полностью локальна.
- Делай только то, что запрошено.

## Acceptance Criteria
- [ ] `capture()` дважды с одним и тем же текстом создаёт один файл, второй вызов возвращает ссылку на существующий
- [ ] `promote()` на тестовой raw-заметке создаёт ≥2 атомарные wiki-заметки со взаимными `[[ссылками]]`
- [ ] Заметка из 20_output открывается в Obsidian и её граф показывает связи с wiki-заметками
- [ ] `recall("<тестовый запрос>")` возвращает релевантные пути за <1 сек на vault текущего размера
- [ ] `git status` чист вне jarvis/

## Action Boundaries
- Чтение vault и создание файлов в трёх целевых папках — выполняй сам.
- Остановись и спроси перед: любой перезаписью или перемещением существующей заметки пользователя, изменениями в `.obsidian/`, установкой плагинов, установкой зависимостей.

## Progress Evidence
Перед изменением логики promote покажи на реальном примере из моего vault: что было на входе и какие заметки получились. Дальше — только после моего «ок».
```

🎯 Target: Claude Code, 💡 Запрет на удаление и перезапись чужих заметок вынесен в начало — это единственный необратимый риск при подключении агента к личному vault.

---

**Промпт 3 — локальный голос**

```
## Objective
Добавить Jarvis локальный голосовой контур: распознавание речи на входе и синтез на выходе, полностью офлайн, с поддержкой смешанной речи украинский / русский / английский.

## Context
- Машина: Mac на Apple Silicon, [УКАЖИ ЧИП И ОЗУ, например M2 Pro / 16 GB]
- Подсистемы jarvis/ (навыки + окружения) и jarvis/memory/ (Obsidian) уже работают
- Я переключаюсь между тремя языками в пределах одного разговора, иногда в пределах одной фразы
- Требование к качеству: голос должен звучать естественно, не как робот-читалка

## Target State
1. Сервис `jarvis/voice/` с локальным HTTP/сокет-API: `POST /listen` (аудио → текст + определённый язык), `POST /speak` (текст → аудио), `GET /health`.
2. Режим push-to-talk по горячей клавише. Hotword — только если не ухудшает задержку.
3. VAD для отсечения тишины и авто-стопа записи.
4. Автоопределение языка на каждом сегменте; TTS выбирает голос под определённый язык.
5. Замер и фиксация в `jarvis/voice/BENCHMARK.md`: задержка STT, задержка первого аудио TTS, потребление памяти — на моей машине, реальными прогонами.

## Constraints и порядок работы
1. СНАЧАЛА исследование, потом код. Проверь по актуальной официальной документации проектов, какие open-source модели сегодня доступны и реально поддерживают украинский, русский и английский. Не полагайся на память о версиях и названиях моделей.
2. Кандидатов рассматривай минимум по 2-3 на каждую сторону, включая варианты с ускорением под Apple Silicon (Metal / MLX / Core ML).
3. Выдай мне сравнительную таблицу до реализации: модель, размер, лицензия, поддержка UA/RU/EN, ожидаемая задержка, качество голоса. Столбцы, где ты не уверен, помечай `[не проверено]`. Не выдумывай бенчмарки и названия моделей.
4. Реализуй только после моего выбора.
5. Всё офлайн: после загрузки моделей сервис обязан работать при выключенном интернете.
6. Никаких облачных API, ключей и телеметрии.

## Scope
- Работать только в: jarvis/voice/ и точке интеграции в jarvis/router/
- Модели складывать в: jarvis/voice/models/ и добавить в .gitignore
- НЕ трогать: jarvis/memory/, код Orbit CRM, системные настройки macOS

## Acceptance Criteria
- [ ] Фраза со смешением языков распознана корректно, язык определён верно для каждого сегмента
- [ ] Задержка от конца речи до начала ответа TTS измерена и записана в BENCHMARK.md
- [ ] Сервис отработал полный цикл при отключённом Wi-Fi
- [ ] Голос TTS оценён на образце ≥30 секунд по каждому из трёх языков
- [ ] Падение микрофона или модели не роняет основной процесс Jarvis

## Action Boundaries
- Код и локальные тесты внутри jarvis/voice/ — выполняй сам.
- Остановись и спроси перед: `brew install`, скачиванием любой модели тяжелее 1 GB, изменением системных разрешений macOS на микрофон, добавлением зависимостей в общий манифест проекта.

## Progress Evidence
Каждый вывод о качестве или задержке подкрепляй фактическим выводом команды. Утверждения вида «работает быстро» без числа не принимаются.
```

🎯 Target: Claude Code, 💡 Выбор моделей вынесен в отдельный этап с проверкой по документации — модели STT/TTS для украинского меняются быстро, и это главный источник фальшивых рекомендаций.

> Промпты 1–3 предназначены для агента с реальным доступом к системе. Перед вставкой сверь пути, папки и права — особенно путь к Obsidian-vault в промпте 2.

---

**Промпт 4 — лицо (Claude Design)**

Сначала прикрепи файлы своего дизайна Orbit CRM, потом вставь это:

```
Ты — продуктовый дизайнер интерфейсов для операторских и командных панелей. Во вложении — существующий дизайн моего продукта Orbit CRM.

ЗАДАЧА
Спроектировать один главный экран персонального ассистента «Jarvis», встроенного в Orbit CRM. Это командный центр: я смотрю на него весь день и управляю голосом.

ШАГ 1 — извлеки из вложенного дизайна и покажи мне списком, прежде чем рисовать:
шкалу цветов, типографику и её масштаб, радиусы, сетку и отступы, стиль иконок, плотность интерфейса. Дальше работай строго внутри этой системы.

ШАГ 2 — экран собирается из шести зон:
1. Ядро голосового состояния — центральный визуальный элемент с четырьмя различимыми состояниями: покой, слушает, думает, говорит. Состояние читается периферийным зрением, без чтения текста.
2. Метрики — 4-6 живых показателей, крупные цифры, дельта к прошлому периоду.
3. Расписание дня — вертикальная лента с текущим моментом, выделенным явно.
4. Входящие и тренды — компактный поток, сворачиваемый.
5. Лента памяти — что Jarvis только что зафиксировал, со ссылками на заметки.
6. Строка команды — текстовый ввод как альтернатива голосу.

ЭСТЕТИКА
Тёмный интерфейс, ощущение глубины через слои и полупрозрачность, а не через тени. Тонкие геометрические линии и концентрические дуги как структурный каркас, а не как декор. Один акцентный цвет, взятый из моей палитры, работает как индикатор состояния — второй акцент только для тревожных значений. Данные — главный источник визуального шума, всё остальное приглушено. Отсылка к научно-фантастическим командным HUD-интерфейсам, но переосмысленная под мою систему, без кинематографического пафоса.

ОБЯЗАТЕЛЬНО
- Не используй логотипы, персонажей, шрифты и любые фирменные элементы существующих фильмов и брендов. Только оригинальная графика.
- Никаких дефолтных дашбордных паттернов: одинаковых карточек-плиток в сетке 3×3, сайдбара с 12 пунктами, стоковых чартов из библиотеки.
- Контраст текста минимум 4.5:1. Тёмная тема не должна съедать читаемость цифр.
- Каждое состояние голосового ядра покажи отдельно.
- Спецификация движения: длительность и easing только для смены состояний и появления данных. Ничего постоянно анимированного.

ВЫДАЧА
1. Главный экран, десктоп 1440px
2. Четыре состояния голосового ядра рядом
3. Адаптив 375px — что схлопывается и в каком порядке
4. Токены: цвета, типографика, отступы, радиусы — в виде переменных
5. Три строки: какое решение приняло каждый ключевой визуальный выбор

Реальные подписи и правдоподобные данные вместо lorem ipsum.
```

🎯 Target: Claude Design, 💡 Извлечение токенов из твоего дизайна вынесено в отдельный первый шаг — иначе генератор нарисует красивый экран мимо твоей системы.